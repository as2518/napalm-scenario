- get_bgp_neighbors:
   global:
    {{ peer_group }}:
      _mode: strict
      {% for neighbor in neighbors -%}
      {{ neighbor.neighbor_address }}
        is_enabled: True
        is_up: True
      {{% endfor -%}}